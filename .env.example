# Infrastructure probes only — not domain configuration.
# They exist to prove that client-public and server-only variables
# are separated by the typed astro:env API. Both are optional.

# Exposed to the client bundle. Never put anything sensitive here.
HARNESS_PROBE_PUBLIC=

# Server-only. Never commit a real value here.
HARNESS_PROBE_SECRET=
